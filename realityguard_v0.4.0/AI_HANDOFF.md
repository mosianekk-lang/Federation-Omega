# AI handoff

Run `PYTHONPATH=src python -m unittest discover -s tests -v` before modifying claims about maturity; the current baseline is 115/115. Use `examples/false_ownership.json`, `examples/chatbridge_solution_request.json`, `examples/chatbridge_prebuild_request.json`, `examples/genuine_gap_prebuild_request.json` and `examples/material_cycle_upgrade.json` as smoke fixtures. Preserve lifecycle ordering, evidence thresholds, deterministic output, fail-closed semantics, truth/solution separation, manifest-hash binding, environment-specific proof, capability preservation, correction-debt ordering, explicit supersession and reuse-before-build ranking. Do not say the system is installed, deployed, running in ChatGPT, live-bound to Federation, or owned as an operational service unless target-system evidence advances those exact states.

At every material build, failure, recovery, deployment or canary boundary in an integrated host, invoke `realityguard.upgrade.v1` automatically. `PATCH_EXISTING` and `CREATE_CANDIDATE` are governed decisions, not execution permits. Require original-failure tests, healthy-case tests, rollback and a separately consumed single-use Formation permit before execution. Keep every Federation system `ADAPTER_REQUIRED` until its real runtime proves the hook.

RealityGuard is also the central fault-book manager. Use `faultbook-import` only with a verified hash-chained ledger and private registry, and publish only `faultbook-manifest` output. Never promote `VERIFIED_SOURCE` or `VERIFIED_INVOCATION` to `VERIFIED_HOST_BOUND` without current host invocation and semantic readback; inaccessible historical chats remain `ADAPTER_REQUIRED`.

The verified ChatBridge solution route is `BLOCK_CLAIM_PRESERVE_OBJECTIVE` plus `COMPOSE`; its pre-build route is `BLOCK_DUPLICATE_BUILD` plus `ADOPT`. The existing `chatbridge-companion` 0.2.0 implements and locally tests the button/capsule source scope and adds a read-only Windows policy-readiness assessor plus a fail-closed enterprise deployment handoff; do not rebuild it. The assessor has not run on the target work laptop. Browser policy readback, extension installation, signed-in session binding, live warning interception and successor-chat semantic readback are lifecycle execution/proof work, not new-source gaps. Never bypass browser or employer policy. Adaptive learning records are TESTED, not REGISTERED or BEHAVIOR_PROVEN.
