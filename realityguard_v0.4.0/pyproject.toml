[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "realityguard"
version = "0.5.0"
description = "Deterministic truth protection, side-effect execution guards, reuse-first routing and governed material-cycle upgrades"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
realityguard = "realityguard.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
