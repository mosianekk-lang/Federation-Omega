# JFRIE / EACIA current-mainline alignment — 12 August 2026

## Version truth

The EvidenceOps canonical policy/control plane is **JFRIE v2.0 / EACIA** (`JFR-LEG-001`) on the canonical Drive/registry surfaces.

The public repository contains the deterministic executable **JFRIE v1.1 referral-autopsy baseline** in:

- `jfrie.py`
- `jfrie_v11.py`
- associated regression tests

A bounded **JFRIE v2 executable assurance foundation** is now proposed in `jfrie_v2.py`. It wraps the v1.1 hard gates with per-gate execution receipts and implements the canonical T001-T012 pollution/regression family plus EA-07 hash-scope and EA-08 role/authority separation controls.

These states must not be conflated. Repository source presence does not prove executable parity with every JFRIE v2.0/EACIA control described in the canonical policy document.

Until full executable v2 parity is independently implemented and tested, use these labels:

- `CANONICAL_POLICY = JFRIE_V2_EACIA`
- `REPOSITORY_EXECUTABLE_BASELINE = JFRIE_V1_1`
- `EXECUTABLE_V2_FOUNDATION = CANDIDATE_UNTIL_ADMITTED`
- `EXECUTABLE_V2_PARITY = UNVERIFIED`

The foundation may advance only through the normal source admission path and then through deterministic, shadow, adversarial and limited-workflow proof. It does not inherit policy maturity merely because it implements selected canonical controls.

## Current EvidenceOps responsibility split

JFRIE operates inside the current EvidenceOps assurance stack as follows:

1. **TruthGrid vNext** — evidentiary finality, contradiction closure, TruthState and decision-readiness state.
2. **LEX-OMEGA v1.1 + current-mainline alignment adapter** — specialist legal/labour/forensic analysis, legal proposition lineage, current-authority control and adversarial counsel synthesis.
3. **JFRIE / EACIA** — fail-closed jurisdiction, legal-route, evidence contamination and release integrity.
4. **CASEFORGE-Ω / SCIENTIA** — scientific falsification, competing hypotheses, blind benchmark scoring, fatal-integrity overrides and candidate evolution testing.
5. **CapabilityResolutionGate** — proof burden before terminal system claims such as CAN, CANNOT and DONE.
6. **EvidenceOps EvolutionGovernor** — existing promotion authority for validated improvements. No legal subsystem self-promotes doctrine or maturity.

## Bounded v2 executable foundation

The candidate foundation is deliberately smaller than the canonical v2 control plane. Its present executable scope is:

- inherited v1.0/v1.1 hard-gate execution with structured EA-02 receipts;
- version-pinned node/readback enforcement for T008;
- derivative-support collapse for T002;
- dispute-date drift blocking for T003;
- transmission-to-knowledge separation for T004;
- silence-to-agreement protection for T005;
- excluded-matter resurrection blocking for T006;
- source invalidation downgrade/recall for T007/T012;
- attachment completeness for T009;
- quarantined-claim reuse blocking for T010;
- generated-detector shadow/no-self-promotion control for T011;
- EA-07 scoped-hash enforcement;
- EA-08 role-versus-authority separation;
- A1/no-external-effect enforcement.

Still open before **full v2 parity**: the complete v2 claim/provenance graph, every C001-C100 executable control, complete detector family behavior beyond the foundation slice, all per-object integrity semantics, full contamination-radius propagation, complete release/render/service receipts, independent second-pass orchestration, provider/runtime integration where applicable, and operational maturity proof.

## Release ordering

For a high-risk legal/evidentiary workflow, the bounded order is:

```text
SOURCE / TRUTHGRID STATE
  -> LEX-OMEGA SPECIALIST ANALYSIS
  -> JFRIE FAIL-CLOSED INTEGRITY GATE
  -> CASEFORGE / SCIENTIA CHALLENGE WHERE MATERIAL
  -> JFRIE RECHECK AFTER MATERIAL REPAIR
  -> RELEASE FIREWALL
  -> CAPABILITY-RESOLUTION CHECK BEFORE CAN/CANNOT/DONE CLAIM
```

CASEFORGE does not replace JFRIE. TruthGrid does not decide law. LEX-OMEGA cannot override JFRIE. CapabilityResolution does not establish legal merits. Each engine keeps its own evidence and authority boundary.

## Maturity

New alignment work uses the current CASEFORGE-compatible ladder:

`DESIGNED -> DETERMINISTIC_TESTED -> SHADOW_VALIDATED -> ADVERSARIALLY_VALIDATED -> CANARY_VALIDATED -> LIMITED_WORKFLOW_VERIFIED -> CROSS_DOMAIN_VERIFIED -> OPERATIONAL_VERIFIED`

Legacy labels remain historical compatibility fields. No mapping itself constitutes promotion.

## Permanent truth boundary

Do not claim:

- JFRIE v2 executable repository parity without complete tested source proof;
- the new v2 foundation is admitted until exact-head Airlock and Leak Guard pass and current-main readback succeeds;
- provider-blind CASEFORGE execution without provider-native readback;
- TruthGrid decision readiness when its completion vector is NOT_READY;
- a terminal DONE state with executable internal dependencies remaining;
- cross-chat activation without canonical readback/synchronisation evidence.
