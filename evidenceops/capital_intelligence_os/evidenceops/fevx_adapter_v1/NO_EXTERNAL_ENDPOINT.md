# No external execution endpoint

The v1 adapter is a library and CLI. It exposes no network action endpoint and contains no send, filing, payment, publication, destructive or production mutation path.
