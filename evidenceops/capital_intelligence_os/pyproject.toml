[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "federation-omega-consolidation"
version = "1.0.1"
description = "Alpha→Omega proof-carrying 24-hour Federation Omega consolidation kernel"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["federation_consolidation", "alpha_omega_v30"]

[tool.pytest.ini_options]
testpaths = ["tests"]
