# AI handoff

Read `FORMATION_SPEC.md`, `PROJECT_MEMORY.md`, `BUILD_CONTRACT.json`, `TEST_COVERAGE_MAP.md` and `README.md` before changing behavior.

## Current implementation

The verified-v4 foundation is vendored unchanged under `foundation/`. `VerifiedV4Authority` is the sole recommendation and trust authority. The catalogue engine, Bible federation, SQLite system, CLI and scheduler are compatibility facades. Static files are synthetic. Turn ingress requires complete signed lineage and a fresh destination-signed receipt. Scheduler and remediation paths are inventory-only.

The runtime service and MCP adapter contain no heartbeat endpoints or tools. Do not claim API, MCP, deployment, active-chat, bidirectional-delivery or system-wide capability from manifests or documentation.

## Required continuation checks

1. Run the 30-test corrected integration suite and preserve the 24-test mapping.
2. Run the complete verified-v4 foundation suite from its canonical source or an exact-byte integrated test copy.
3. Run the exact enumerated 55-test Federation Omega compatibility command.
4. Run static, MODISA contract, documentation, public-leak, compile and diff checks.
5. Red-team forged/future/stale lineage, owner/matter/classification drift, rotation, stop generation, receipt freshness, raw content, fixture ingress, replay conflicts, false live attachment and scheduler authority.
6. Treat API, MCP, durable storage, deployment and live activation as separate later missions.

## Prohibited continuation

Do not add static keys, implicit signers, raw chats, task summaries, personal/legal content, network calls, provider SDKs, background threads, heartbeat cron recommendations, catalogue-authorized ingress, effectful routes, auto-execution, owner impersonation or stronger maturity claims.
