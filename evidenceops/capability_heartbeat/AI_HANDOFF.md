# AI Handoff

Run `python -m unittest discover -s evidenceops/capability_heartbeat/tests -v`, the sovereign-runtime tests and `npm run check` in `evidenceops-mcp-adapter` before changing behavior. Preserve stable event IDs, monotonic per-chat sequences, privacy projection, atomic outbox receipts, PRE-before-POST seed enforcement and adapter-remediation truth states. Never mark a surface live from source existence, local tests, deployment configuration or a tool scan; require a provider-backed turn and delivery readback.
