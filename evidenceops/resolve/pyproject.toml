[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "evidenceops-resolve"
version = "1.0.0"
description = "Provider-agnostic resilient evidence execution and independent verification"
requires-python = ">=3.11"
authors = [{name = "Kim Kagiso Mosiane"}]
dependencies = []

[project.scripts]
evidenceops-resolve = "resolve.cli:main"

[tool.setuptools]
packages = ["resolve"]
