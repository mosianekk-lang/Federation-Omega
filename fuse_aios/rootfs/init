#!/bin/busybox sh
set -eu
/bin/busybox --install -s /bin
/bin/mount -t proc proc /proc 2>/dev/null || true
/bin/mount -t sysfs sysfs /sys 2>/dev/null || true
echo "FUSE_AIOS_BOOT_OK arch=$(/bin/uname -m) pid=$$"
/bin/sync
while true; do /bin/busybox sleep 3600; done
