# AI handoff

Continue from the isolated shadow branch. Run both test suites, compare the sentinel
against the existing companion using identical synthetic and real rendered-message
fixtures, and publish no performance claim without measured results. Do not activate
the extension, merge to main, or deploy a worker without separate authority and exact
readback.

The native Edge adapter is code-complete only, not deployed. Its configuration
must remain disabled until an authorized Edge hook is independently built,
permission-reviewed, canaried and read back. Never pass derived health values as
route modes; only `ACTIVE` and `PASSIVE` are accepted inputs.

The `edge_extension` directory is source-only and deliberately inert. Do not
invent an extension ID or update URL, apply enterprise policy, load it unpacked,
grant optional permissions, or send `FACPF_ATTACH` without a separately
authorized installation and canary cycle.
