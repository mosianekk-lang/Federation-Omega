[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "federation-omega-v2-core"
version = "0.1.0"
description = "Event-sourced world model, deterministic intent compiler, and canonical mission-query API."
requires-python = ">=3.11"

[project.scripts]
federation-omega-v2 = "federation_omega_v2.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["federation_omega_v2*"]
