# Phase 2 architecture

CANONICAL REGISTER → SYSTEM EVENTS → HASH CHAINS → RELATIONSHIP GRAPH
→ CANONICAL QUERY → SYNTHETIC EVIDENCEOPS MISSION → RESTART PROOF

The event log is authoritative. Relationships use deterministic IDs. Duplicate
replays are idempotent; conflicting identity reuse fails closed.
