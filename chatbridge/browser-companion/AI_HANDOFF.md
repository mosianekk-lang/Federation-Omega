# AI handoff

Resume from `PROJECT_MEMORY.md`, run `npm run check`, then inspect any current ChatGPT DOM changes before altering selectors. Preserve the deterministic core and privacy contract. Never remove the native fallback or store full transcripts in durable local storage. Publish changes through an isolated GitHub branch and require a live browser readback before promoting `deployed` or `proven`.
