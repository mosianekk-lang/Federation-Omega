{
  "name": "chatbridge-companion",
  "version": "0.2.0",
  "private": true,
  "description": "Manifest V3 ChatBridge handoff companion for ChatGPT",
  "scripts": {"test": "node --test tests/*.test.js", "check": "node --check src/bridge-core.js && node --check src/background.js && node --check src/content-script.js && node --check options/options.js && npm test"},
  "engines": {"node": ">=20"},
  "license": "UNLICENSED"
}
