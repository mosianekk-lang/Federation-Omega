{
  "name": "chatbridge-companion",
  "version": "0.3.0",
  "private": true,
  "description": "Manifest V3 ChatBridge Ω4.9 full-fidelity browser capture and successor replay adapter for ChatGPT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check src/bridge-core.js && node --check src/edge-egress.js && node --check src/background.js && node --check src/content-script.js && node --check options/options.js && npm test"
  },
  "engines": {"node": ">=20"},
  "license": "UNLICENSED"
}