# AI handoff

Resume from `PROJECT_MEMORY.md`, run `npm run check`, then inspect any current ChatGPT DOM changes before altering selectors. Preserve the deterministic core, no-admin policy assessor and privacy contract. Never remove the native fallback, store full transcripts in durable local storage, request elevation, mutate enterprise policy or bypass a blocked extension route. Use the prepared enterprise handoff when policy requires managed deployment. Require target-browser readback before promoting `deployed` or `proven`.
