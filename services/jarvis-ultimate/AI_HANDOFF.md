# AI handoff

Continue from the stacked JARVIS 1.5.0 candidate. The security, semantic-proof, signed-permit and anchored-state foundation is v1.4.0; T20 is an additive execution layer. Do not rebuild the core, replace it with the archived simplified v1.0 branch, weaken an action schema, accept caller booleans as completion proof, or add a new public workflow.

Before external work:

1. Read `BUILD_CONTRACT.json`, `LESSON_GATE_72H.json`, `FORMATION_SPEC.md` and `PROJECT_MEMORY.md`.
2. Re-run the locked installed-package tests and doctrine invariants at the exact candidate commit.
3. Verify current provider identity, authority, model/configuration and semantic-readback requirements.
4. Obtain a current single-use Formation decision for the exact mutation.
5. Preserve the mandatory next best automated pathway and the route-level success/failure/no-op ledger.

The T20 envelope governs one bounded attempt:

- split at 720 seconds;
- stop scope expansion at 900 seconds;
- enter release-only mode at 1,080 seconds;
- emit an honest terminal receipt at 1,200 seconds.

Do not turn deadline expiry into success. A completion or speed-improvement claim requires fresh structured quality evidence, independent adversarial proof, semantic readback, known-failure replay, a meaningful state delta, and an executable next route. Speed improvements remain shadow candidates and never self-promote.

The dependency lane is source-complete only when `uv lock --check`, `uv sync --frozen`, the installed-package regression, the full local suite, and the JARVIS step in the allowlisted Federation Airlock all pass for the same commit. The compatible direct set remains `google-adk==2.1.0`, `google-genai==1.75.0`, and `cryptography==46.0.0`.

The next authorized engineering lane after same-commit v1.5 proof is provider identity, not feature expansion: repair and read the machine identity, run a bounded Gemini read canary, then deploy a private zero-traffic Cloud Run candidate. Reject generic HTTP health. Require two stable action-specific semantic results plus exact source→build→digest→revision→traffic lineage. Capture the prior revision and test rollback before promotion.

Workspace is a separate authority lane. Use incremental OAuth, exact user identity and minimal scopes. Do not assume ChatGPT Drive/Gmail connectors, Google Cloud IAM, Apps Script sharing, API keys or service-account ownership provide Workspace user-data authority.

`gmail.send` and `gmail.forward` require an explicit current owner instruction and the full executor-only authority/permit chain. Do not send or forward email from a draft, plan, prior approval, inferred urgency or connector availability.

The pre-gate PR 548 head is preserved at `archive/jarvis-t20-pre-72h-gate-20260817`. The active T20 branch must remain stacked on `agent/jarvis-ultimate-v1-1` until the v1.4 foundation is merged or PR 548 is deliberately retargeted after current-state verification.

If any gate fails, preserve successful routes, quarantine only the affected lane, record the failure append-only, materially reroute, and continue the original mission without a live claim.
