# Sol + EvidenceOps Unified Autonomy Runtime v7.0.0 — hosted proof package

This directory carries a chunked, base64-encoded copy of the exact verified release archive:

```text
OmegaMax_Sol_EvidenceOps_Unified_Autonomy_Runtime_v7.0.0.zip
SHA-256: a070c7adf6523b20e43389de972952785713b9f586326867ea46c8b465f4f6ca
```

The hosted GitHub Actions proof reconstructs the archive, verifies its hash and internal manifest, installs its wheel, executes all 98 tests, runs the core/companion/unified-system canaries, executes programme phases P00–P06, verifies P07–P09 remain provider-gated, starts the installed HTTP service, verifies readiness and a task-bound system operation, and uploads the provider proof package.

The workflow proves the exact archive on a GitHub-hosted worker. It does not prove deployment of Temporal, NATS JetStream, OPA, PostgreSQL, a long-running external worker, a real legal corpus workflow, or live financial execution.
