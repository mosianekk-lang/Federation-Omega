[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sovereign-execution-boundary"
version = "0.1.0"
description = "Provider-neutral, policy-governed AI workflow control plane"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
seb = "seb.api:main"

[tool.setuptools]
packages = ["seb"]

