# Secondary Brain Canonical Query Contract

The Secondary Brain becomes a read-only canonical resolver. For every task it returns one canonical system ID, version, source, owner workstream, current state, last proof, duplicate risk, recommended route, open gates and confidence. A conflict object is returned instead of guessing when multiple live authorities exist.
