# AI Handoff — BCO-Prime Chat Forensics v1

Current contract:

- Preserve exact conversation ID and source hashes.
- Prefer native export. When unavailable, label DOM and execution-trace evidence as fallback evidence.
- Keep FINAL_OUTPUT_COMMIT_FAILURE separate from the exact backend cause.
- Treat duration, connector count, console noise, and context proxies as contributors, never causal proof.
- Declare provider durability only when a pinned provider reference contains matching artifacts.
- Keep the extension pure and deterministic; external acquisition belongs to separately authorized adapters.
- Keep the canonical BCO-Prime core at exactly 100 functions.
- Never self-promote the competency. Registration and operational proof require explicit subsequent gates.

Next verification sequence: run the original-incident fixture, all extension tests, the canonical core tests, syntax compilation, contract validation, deterministic replay, and provider readback after publication.
