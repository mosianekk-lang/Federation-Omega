# Proof-scope reminder

The supplied Omega-One v0.8.4 blueprint defines 100 named capabilities. The supplied Startup Register also preserves earlier rows explicitly stating `STATIC_CANDIDATE_REGISTRY_TESTED / NO_100_CAPABILITIES_DEPLOYED`, and later rows describing a staged 100-improvement package with 16/16 hermetic unit tests. The maturity compiler exists to reconcile those layers rather than let an aggregate activation label silently promote every capability to deployment/provider/value maturity.
