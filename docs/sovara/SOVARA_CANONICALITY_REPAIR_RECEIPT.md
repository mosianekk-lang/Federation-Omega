# SOVARA canonicality repair receipt

Date: 2026-08-19

State: `SOURCE_PROJECTION_READY / NO_PROVIDER_DEPLOYMENT_CLAIM`

This public-safe repair records:

1. `A6-CFR` as the controlling SOVARA Constitutional Federation Runtime identity.
2. `A6-ADR` as preserved historical/pre-consolidation lineage despite sharing the same semver label.
3. Restore resolution by role + immutable SHA-256 + lineage rather than semver alone.
4. SOVARA as mission/authority/proof control plane; Federation systems remain preserved and independently identifiable.
5. Secondary Brain as a read-only canonical resolver/context assembler, not a competing authority root.
6. Provider applications and AI models remain replaceable adapters.
7. Public GitHub remains source/CI/provenance only and is not a private sovereign backup root.

No Cloud Run deployment, IAM mutation, secret access, email, public exposure of private material, production traffic change or provider-runtime promotion is claimed by this receipt.
