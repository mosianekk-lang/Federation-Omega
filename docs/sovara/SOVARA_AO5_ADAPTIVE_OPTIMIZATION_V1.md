# SOVARA Ω × JARVIS ΑΩ5 — Adaptive Cognitive Execution Governor v1

## Purpose

This is an additive challenger extension to the full zero-dilution ΑΩ5 workstream in PR #699. It does not replace ΑΩ5, SOVARA, RealityGuard, CFBE, Sentinel or JARVIS assurance. It composes their strongest execution genes into one hot-path governor for platform routing, chat/context optimization and adaptive correction.

## Federation capability genes adopted

- ΑΩ5 execution budgets, information-gain doctrine, throughput governor, context sentinel, near-miss learning, recurrence law, Ω-Scientist experiment/promotion gates, RealityGuard receipt and AutoFix.
- SLOS adaptive route posterior / value-of-information / failure-domain-portfolio concepts.
- Session rebase and split-brain fencing for moving state and concurrent effectful work.
- Federation output-cadence guard: early material-value release, plus checkpoint pressure after five tool operations or seven minutes without visible output.
- Proof freshness as a hard route-eligibility gate.
- Trajectory learning from failures and owner corrections.
- Protected context compaction that may remove transient duplication but never adverse evidence, contradictions, gaps, active blockers, verified facts, decisions or source references.

## Operating path

`CURRENT PROOF → ROUTE POSTERIOR → FAILURE-DOMAIN PORTFOLIO → VALUE-OF-INFORMATION PROBE → SESSION/SPLIT-BRAIN FENCE → CHAT PRESSURE/CADENCE CONTROL → SAFE COMPACTION/CHECKPOINT → FAILURE/NEAR-MISS CORRECTION → SHADOW → FORWARD CANARY → INDEPENDENT READBACK → SCIENTIST PROMOTION GATE → SOVARA EFFECT ADMISSION (separate)`

## Hard boundaries

The optimizer may change routing, search ordering, scoring, context controls, recovery methods and output structure only. It may not weaken or silently modify the immutable kernel, consequence gate, owner authority, source supremacy, proof standards, legal-safety controls, adverse evidence or active gaps/blockers.

No external/provider effect executor exists in this module. It cannot create credentials, IAM authority, deployment authority, scheduling authority, outbound communication authority or production claims. SOVARA remains the sole effect-admission plane.

## Local verification

Focused local optimizer suite: **26/26 PASS**.

Deterministic adaptive canary: **21/21 PASS**, including stale proof rejection, fresh champion selection, anti-correlated shadow selection, VOI discriminator selection, session rebase, stale effectful hold, chat checkpoint/fast release, protected compaction, recurrence escalation, near-miss learning, promotion proof gates, RealityGuard receipt and zero external effects.

A synthetic matched benchmark models a reduction from 1.0 to 0.6 normalized context cost and 1.0 to 0.7 normalized tool cost. This is synthetic deterministic evidence only. It is not provider performance, production performance, a 10× claim, or a future-performance guarantee.

## Admission strategy

This extension is intentionally stacked on `feat/sovara-ao5-full-zero-dilution-v2` / PR #699. The existing AO5 workflow already watches `ops/sovara_ao5_full_v2/**`, so hosted CI can run the complete incumbent ΑΩ5 tests together with the new optimizer regressions without adding another workflow.

The challenger must not be promoted to current stable merely because its local tests pass. Required next proof is hosted stacked-PR CI, unchanged 55/55 zero-dilution coverage, full incumbent regression success and then real-mission/behavioral evidence before any performance promotion.
