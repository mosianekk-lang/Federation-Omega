# FUSE Genesis Sovereign Hardening v2

Status: F321 source-admission candidate under the existing FUSE Sovereign Plane R55, Alpha-Omega mission compiler, Formation Innovation challenger, FDOF source fencing, and independent Reality Judge.

## Purpose

This tranche source-admits the already source-independent-qualified residual mechanisms required to move operational control away from any single AI or infrastructure provider: resident execution, encrypted content-addressed custody, workload identity and secrets, durable event transport, private-source forge/bootstrap, local-model qualification, and creative-runtime qualification.

It extends the F320 Genesis sovereign runtime core admitted on signed main `e13e6de83d55d8620fab4ba222d3cf8a79215398`; it does not create a second controller, scheduler, truth/memory/proof/authority root, or Judge.

## Included mechanisms

- `resident_host.py`: durable carrier state, fenced task/checkpoint lifecycle, restart/recovery semantics.
- `encrypted_cas.py`: encrypted content-addressed object custody, integrity/AAD/replica/restore checks.
- `identity_secrets.py`: key-reference identity, Ed25519 signatures, bounded tokens, rotation/revocation, encrypted versioned secret custody.
- `event_fabric_v2.py`: ordered/idempotent durable events, ACK/redelivery, pause/resume, retry/DLQ, replay and restart persistence.
- `forge_bootstrap.py`: signed-tree/source manifest checks, mirror/offline-bundle verification, divergence detection and proof-gate requirements.
- `model_admission.py`: exact model hash/license/source/format/quant/context + hardware-fit/runtime/matched-benchmark admission TCK.
- `creative_tck.py`: provider-neutral creative-cell qualification for modality, licensing/provenance, editability, technical/fidelity QC and targeted rerender.

## Verification boundary

Before repository mutation, the seven preserved packages were re-executed from their own package roots and passed 250/250 deterministic tests in aggregate (20 + 30 + 40 + 40 + 40 + 40 + 40). The repository-layout candidate is re-tested after import rebinding and must pass its exact focused suite and `compileall` before publication. Hosted exact-head Airlock/ProofOS, Bubbles and Public Repository Leak Guard are still required before source admission.

Source admission does **not** establish persistent 24x7 deployment, physical Windows task callability, live local-model inference, live local rendering, provider-loss survival, owner value, or COMPLETE. Those remain separate runtime/physical/recovery/Judge predicates.

## Windows currentness boundary

`HOST_SESSION_STATE`, local service/listener state, broker binding, transport callability and task-roundtrip proof are separate. A stale/offline broker projection must not be promoted into a claim that the owner PC or Desktop Commander service is offline. Current routing must REBIND/REPAIR existing carriers before building another transport.

## Authority

F321 is source-only. No provider/IAM/deployment/traffic/public invocation, security-policy mutation, external communication, unknown spend, or destructive effect is granted by this tranche. Expiry is never release; source mutation must remain within the exact F321 scope and current fence.
