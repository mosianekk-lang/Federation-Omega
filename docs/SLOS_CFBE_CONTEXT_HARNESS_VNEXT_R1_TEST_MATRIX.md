# SLOS CFBE Context + Harness vNext R1 Test Matrix

Required focused tests:
- context tournament deterministic budget selection
- mandatory context budget fail-closed behavior
- context-policy experiment identity mismatch
- context-policy quality/readback regression fence
- harness genome deterministic identity
- harness experiment identity binding
- harness hard-regression exclusion before Pareto selection
- unique non-dominated harness advancement
- import/construction smoke test

Required repository gates before admission remain the existing CI, Airlock, ProofOS and Leak Guard suites. This file records intended coverage only and is not itself proof that those gates passed.
