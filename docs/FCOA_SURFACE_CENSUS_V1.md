# FCOA Multi-Surface Reachability & Control Census v1

This control closes the failure where one offline connector was mistaken for an offline Windows environment.

Rules:
- ROUTE_OFFLINE != DEVICE_OFFLINE.
- USER/SCREENSHOT OBSERVED ACTIVE != CONTROL REACHABLE.
- ONE CONNECTOR FAILURE != CAPABILITY ABSENT.
- Before absence claims, census direct GUI/computer-use, local helper, remote terminal, Win32 injection, LocalLLM bridge, VM/browser and future qualified routes.
- A reachable terminal may bootstrap FCOA Device-Control or bounded Win32 SendInput/screen capture.
- Full keyboard/mouse access is claimed only after live authorized input plus screen/foreground/cursor readback.
- Keyboard/mouse reachability does not enlarge provider or external-effect authority.

The 2026-09-21 incident is classified as WINDOWS_11_OBSERVED_ACTIVE with REMOTE_DESKTOP_COMMANDER_ROUTE_UNREACHABLE, not DEVICE_OFFLINE.
