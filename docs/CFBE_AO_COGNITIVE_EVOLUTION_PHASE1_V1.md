# CFBE Ω / Alpha→Omega Cognitive Evolution — Phase 1

Status: **SOURCE CANDIDATE / EFFECT NONE / 10X UNPROVEN**

## Purpose

Source-admit only the residual Phase-1 semantics identified by the 2026-09-09 AO-CEF deep-harvest/equivalence court, without creating another sovereign system.

This slice extends the existing CFBE scientific capability compiler and Alpha→Omega/Formation estate with:

- Cognitive Evolution Genome IR (CEG-IR);
- immutable outer mission/authority/data/compute envelopes;
- quality-diversity retention of elites, stepping stones, failures and anti-patterns;
- verifier-ecology quorum, hidden-held-out, anti-tamper, safety/reliability, provenance/rollback and transfer gates;
- reuse-first cold-slate mechanism routing using the existing `omega_harvest_max_v2` equivalence/superset functions;
- a falsifiable 10X Composite Frontier court based on measured Cognitive Evolution Yield (CEY).

## Reuse-first boundary

The deep-harvest equivalence result classified:

- Verifier Ecology → `PARTIAL_EXTEND`;
- Cognitive Evolution Genome IR → `RESIDUAL_MINIMUM_BUILD`;
- Quality-Diversity Archive → `RESIDUAL_MINIMUM_BUILD`;
- Cross-Substrate Transfer Court → `PARTIAL_EXTEND`.

Accordingly this source slice does **not** recreate ProofOS, CFBE, Formation, AO-Harmonic, a scheduler, memory root, provider executor, truth store, proof plane, or authority plane.

## Architecture

```text
PUBLIC FRONTIER EVIDENCE
  -> existing CFBE Ω-HARVEST MAX v2
  -> mechanism/equivalence/superset court
  -> CEG-IR under immutable OuterEnvelope
  -> diverse Candidate population
  -> existing assurance + verifier-ecology extensions
  -> QualityDiversityArchive
       ELITE
       STEPPING_STONE
       FAILURE
       ANTI_PATTERN
  -> transfer + replication gates
  -> shadow/canary/value layers outside this source slice
```

## Security / authority invariants

- production self-mutation is prohibited;
- a candidate cannot grant itself authority;
- benchmark/held-out leakage fails closed;
- score tampering fails closed;
- rollback and provenance are required;
- independent verifier-domain and evaluator-family quorum are required;
- alternate-model, alternate-harness and adjacent-task transfer are explicit gates;
- provider, IAM, OAuth, spend, traffic, publication and model-training effects remain separately authorized and read back;
- source existence does not imply runtime, provider, neural-training, operational or value maturity.

## 10X truth rule

`TEN_X_COMPOSITE_FRONTIER` is not a design label. The source court can only certify that supplied evidence satisfies its mechanical contract. A real 10X claim requires, at minimum:

1. verified candidate assurance;
2. `SUSTAINED_VALUE_VERIFIED` evidence;
3. CEY ratio >= 10.0 against a frozen comparable composite-frontier baseline;
4. at least two independent replications;
5. no safety/reliability/transfer/provenance regression.

No 10X claim is made by this Phase-1 source admission.

## Provenance

Private AO-CEF Phase-1 reference engine: Drive `1nEWBt4XxSguh83VEm9j3pChdrquZpOQkRcTXrPGGr6U`, 59/59 deterministic reference courts, SHA-256 `71ed65e6be1dd45831bab4f1c2b337c0ba40f42df4dfc173c8329574b74163d2`.

Cold-slate blueprint: Drive `1hdSHeuZZBTiknIEmnYnIUOY0-Z4fTr60uZ144sU-mxA`.

These private artifacts are design/reference provenance only; this repository slice must earn its own exact-head CI admission.
