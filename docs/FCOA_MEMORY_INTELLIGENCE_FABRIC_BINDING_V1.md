# FCOA Canonical Memory & Intelligence Fabric Binding v1

Status: source-bound integration of the existing FCOA-Ω identity into the existing CFBE Bible Memory Fabric. This does not create a second memory root.

## Controlling doctrine
`docs/CFBE_BIBLE_MEMORY_FABRIC_V1.md` remains controlling: immutable event truth -> current verified projection -> derived interpretation.

## FCOA role
FCOA-Ω is both a consumer and a governed producer:
- consumes portable verified global events and projections;
- uses those events as cognition/routing/recovery context;
- emits source/proof/causal-lineage-bound intelligence proposals;
- cannot self-verify a proposal into canonical truth;
- independently accepted proposals become DERIVED_VERIFIED global intelligence events;
- verified updates fan out to all active registered FUSE receivers dynamically;
- receivers ACK the exact event digest and convergence remains explicit.

## Privacy and authority
Only P1/global portable metadata and pointers belong in global memory. P2/P3/P4 raw/private content remains domain-local. Memory state never grants provider credentials, publication, messaging, spend, IAM, destructive-effect or lease-release authority.

## Failure semantics
Projection/fanout failure never rewrites immutable event truth. Duplicate publication is idempotent. Digest mismatch fails closed. Newly registered receivers enter subsequent fanout automatically.
