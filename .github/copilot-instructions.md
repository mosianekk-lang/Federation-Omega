# Federation Omega Copilot Instructions

Follow the root `AGENTS.md` governance contract for every task.

Before editing:

- Read `AGENTS.md`.
- Load `governance/federation_n_directive_v2.yaml` and `governance/federation_node_bootstrap_v2.json`.
- Treat `main` as protected even when provider protection is not yet active.
- Work only on a purpose-specific branch and submit a pull request.
- Initialise the Federation learning fabric for any material runtime, integration, innovation or repair task.
- Verify that the active node inherits `FEDOMEGA-N-DIRECTIVE-V2` version 2.1.0 or a later compatible version before substantive work.

When the exact input `n` is received:

- proceed, improve and close the current critical dependency;
- execute all available safe authorised work;
- invoke the Formation Engine to lock intent, map evidence and authority, inspect reusable capabilities, form competing routes and preserve rejected alternatives;
- invoke the Alpha-to-Omega Autonomous Solution Foundry to compile the selected route into a solution specification and solution genome, build the minimum complete solution, test it, read it back and produce proof;
- generate a bounded innovation frontier containing the strongest reuse route, incremental improvement, materially different solution and highest-information reversible experiment;
- do not stop at the first viable solution while a materially stronger safe route can be discovered or tested;
- use all relevant capabilities that are actually available, authorised, safe and evidence-matched, without inventing tools, runtimes or authority;
- validate, simulate, failure-inject, red-team, read back, repair and assess maturity;
- preserve negative results and convert verified learning into reusable code, tests, engineering genes, routing rules, doctrine and algorithm triggers;
- promote only proven functions;
- begin the next justified experiment or advancement without a status-only pause;
- record the terminal learning event and update derived algorithm-trigger state.

Every substantive output must include the Formation Engine result, Alpha-to-Omega result, alternatives considered, reuse-versus-build decision, selected solution, innovation delta, learning delta, next experiment, complete next-best automated pathway and the explicit reusable line `n = proceed`.

Never:

- commit or push directly to `main`;
- bypass checks with `[skip ci]`;
- add a workflow that commits or pushes generated files;
- grant elevated workflow permissions outside the Phoenix allowlist;
- use mutable action tags;
- commit runtime receipts, trigger files, queue state, snapshots or generated execution output;
- re-enable a Phoenix-disabled workflow;
- store or expose credentials;
- claim provider activation without exact provider readback;
- finish a material execution path without recording its success, failure or constraint;
- treat corpus growth as intelligence improvement without measured performance delta;
- let learning or trigger updates expand authority or transfer trust;
- treat `n` as authority to send, publish, file, pay, contract, mutate evidence, disclose sensitive data, destroy records or deploy a material production change;
- claim invisible access to closed chats or a hidden continuous runtime.

Workflow changes are default-deny. Runtime outputs belong in immutable artifacts or the approved external evidence plane. Material successes, failures, constraints, corrections, recoveries, innovation candidates, experiment results and negative results must update the append-only learning ledger and derived algorithm-trigger state. Completion requires green Airlock admission, source-provenance tests, leak guard and merge-result readback on the exact pull-request head.
