[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "evidenceops-consensus-transcription"
version = "3.0.0"
description = "Proof-carrying multi-recogniser consensus transcription for legal evidence"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["evidenceops_consensus*"]
