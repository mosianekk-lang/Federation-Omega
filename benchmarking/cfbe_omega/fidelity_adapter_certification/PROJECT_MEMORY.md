# Project Memory

Wave 1 was selected after the existing hosted-shadow court proved 30/30 structural pairs and 99.679736% median context reduction without stable promotion. The four-surface lane reuses that kernel and adds only the missing comparative adapter orchestration and independent verifier.

Protected boundaries: no credential or authority inheritance, no provider write, no stable promotion, no generated receipt in canonical source, and full-doctrine rollback preservation.

The GitHub authenticated-profile read may vary independently from repository reads. A successful repository read plus a failed profile endpoint is recorded as `PASS_WITH_ROUTE_VARIANCE`, never hidden or promoted to universal connector health.
