# Omega-One additive runtime utilities

This package contains branch-only, non-effect utilities for the Omega-One v0.8.5 maturity and interoperability candidate.

It does not own execution authority. It does not make network calls. It does not resolve credentials. It does not promote main or production state.
