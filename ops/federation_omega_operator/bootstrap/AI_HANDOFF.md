# AI handoff
Test and leak-scan first. Publish only by purpose branch and PR. Never claim provider, deployment or binding live without native readback. Never request or commit long-lived service-account keys.
