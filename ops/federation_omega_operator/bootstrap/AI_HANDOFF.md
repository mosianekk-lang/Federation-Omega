# AI handoff
Test and leak-scan first. Keep the public source repository zero-OIDC; materialize the deployment workflow only in a separately governed private Ops repository. Never claim provider, deployment or binding live without native readback. Never request or commit long-lived service-account keys or private Drive pointers.
