# Project memory
Public source is mosianekk-lang/Federation-Omega and remains zero-OIDC. The intended trust target is a separately governed private Federation Ops repository, which is not present in the current GitHub installation. Failure fingerprint: STS invalid_target. Provider execution remains conditional on private-repository identity plus trusted Google machine authority.
