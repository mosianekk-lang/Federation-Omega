# Formation specification
Objective: restore unattended keyless GitHub-to-Google deployment for the existing operator and CFRE runtime.
Apply mode requires an already trusted Google machine identity; GitHub OIDC cannot create its own absent provider. Completion requires provider readback, successful token exchange, operator revision readback, live allowlist proof, CFRE bind receipt, private runtime health and rollback evidence.
