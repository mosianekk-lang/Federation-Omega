# EvidenceOps v7.2.2 — P12 Scoped Provider Worker

This directory contains a provider-native GitHub Actions canary for the next verified action in the **Workstream Activation and Capture** checkpoint.

It proves, within a reversible A0/A1 scope:

- hosted provider execution;
- checkpoint integrity;
- simulated crash and deterministic resume;
- persistent primary state;
- replicated state and semantic readback;
- allow-and-deny policy logging;
- liveness and readiness;
- rollback canary; and
- immutable proof receipts with zero external effects.

It does **not** prove or grant production Temporal, NATS, OPA, PostgreSQL, KMS, DNS, external messaging, legal filing, live hearing recording, live financial action, destructive mutation, credential access, or provider-administration authority.
