# Optional hosted-runtime adapter dependencies, outside the core uv.lock.
# Not installed or invoked by offline qualification; qualify this path separately before hosting.
google-cloud-secret-manager==2.30.0
google-crc32c>=1.7.0,<2
