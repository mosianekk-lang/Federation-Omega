[project]
name = "omega-max-operational-runtime"
version = "1.0.0"
description = "Proof-gated GitHub-native Omega-Max queue consumer and repair runtime"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
omega-max-runtime = "omega_max_runtime.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
