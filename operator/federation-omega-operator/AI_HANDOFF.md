# AI Handoff

Continue under N = benchmark, improve, seek/exploit safe opportunities, proceed
to verified end. Pause only at a real external trust boundary.

Start: node --check src/*.js; node --test; node scripts/check-build-contract.mjs.

Do not call this exact production source; run the historic installer; patch the
PFRD/control-plane archives as operator source; weaken boundaries; infer the
deploy adapter; merge or deploy without separate authority.

Next best route: publish a draft PR, read every blob from its exact head, and
run dependency-free CI. Production closure needs authorized build/source
provenance, adapter tests, and an image-only no-traffic canary.
